#!/bin/bash

build_gcc-post () {
#*******************
export PATH=$PATH_HOLD
rm -rf /opt/gnat

if [ -f gcc.SlackBuild.old ]; then
	cp -v gcc.SlackBuild.old gcc.SlackBuild
fi

cd /scripts || exit 1
}

build_gcc-post

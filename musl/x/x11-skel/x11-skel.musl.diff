diff -ruBN /tmp/x11-skel/x11-skel.SlackBuild /source/x/x11-skel/x11-skel.SlackBuild
--- /tmp/x11-skel/x11-skel.SlackBuild	2021-01-05 21:06:02.000000000 +0000
+++ /source/x/x11-skel/x11-skel.SlackBuild	2021-01-06 05:53:32.000000000 +0000
@@ -45,7 +45,7 @@
 fi
 
 if [ "$ARCH" = "x86_64" ]; then
-  LIBDIRSUFFIX="64"
+  LIBDIRSUFFIX=""
 else
   LIBDIRSUFFIX=""
 fi
diff -ruBN /tmp/x11-skel/x11-skel.SlackBuild.log /source/x/x11-skel/x11-skel.SlackBuild.log
--- /tmp/x11-skel/x11-skel.SlackBuild.log	1970-01-01 00:00:00.000000000 +0000
+++ /source/x/x11-skel/x11-skel.SlackBuild.log	2021-01-06 05:53:32.000000000 +0000
@@ -0,0 +1,45 @@
+
+Slackware package maker, version 3.14159265.
+
+Searching for symbolic links:
+
+No symbolic links were found, so we won't make an installation script.
+You can make your own later in ./install/doinst.sh and rebuild the
+package if you like.
+
+This next step is optional - you can set the directories in your package
+to some sane permissions. If any of the directories in your package have
+special permissions, then DO NOT reset them here!
+
+Would you like to reset all directory permissions to 755 (drwxr-xr-x) and
+directory ownerships to root.root ([y]es, [n]o)? n
+
+Creating Slackware package:  /tmp/x11-skel-7.7-x86_64-6.txz
+
+./
+etc/
+etc/X11/
+etc/X11/xorg.conf-vesa
+etc/xdg/
+install/
+install/doinst.sh
+install/slack-desc
+usr/
+usr/bin/
+usr/bin/xorgsetup
+usr/bin/xwmconfig
+usr/lib/
+usr/lib/X11/
+usr/man/
+usr/man/man1/
+usr/man/man1/xwmconfig.1.gz
+usr/share/
+usr/share/xsessions/
+usr/share/xsessions/xwmconfig.desktop
+var/
+var/log/
+var/log/setup/
+var/log/setup/setup.xwmconfig
+
+Slackware package /tmp/x11-skel-7.7-x86_64-6.txz created.
+
